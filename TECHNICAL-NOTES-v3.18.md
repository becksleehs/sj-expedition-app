# FRESH v3.18 구현·검증 메모

- 기존 v3.16 소스에 v3.17 캐릭터 SVG 13개를 복원하고 기능을 통합했다.
- 진행 기록은 CAS(onlyIfMatch / onlyIfNew)로 갱신하며 보상 ID와 XP를 하나의
  레코드에 저장한다. 기존 reward 레코드는 최초 접근 때 이관한다.
- 제출물을 먼저 영구 기록하고 참여자의 보상을 정산한다. 중간 실패 시 재제출 또는
  진행 상태 조회가 미정산 보상을 복구한다. 같은 ID는 다시 더하지 않는다.
- 학생의 save 요청은 클라이언트 XP를 받지 않고 서버 XP로 성장 가능 여부를 판단한다.
- 영상은 1 MiB 조각으로 저장하며 구간 읽기 응답도 최대 1 MiB이다.
  브라우저 영상 길이 검사 및 서버의 선언 길이/파일 크기 검사를 사용한다.
  서버에서 코덱 재인코딩이나 영상 길이의 독립 파싱을 수행하지 않는다.
- 파일 MIME 허용 목록과 실제 컨테이너 시작 바이트를 확인한다.
- 미션 제출물은 관리자 또는 해당 참여자에게만 목록을 반환한다. 읽기 URL에는
  추측하기 어려운 파일별 키가 포함된다. URL을 공유하면 받은 사람도 읽을 수 있다.
- 일반 교사는 공개된 활동을 열람하지만 관리자 PIN 인증을 대신하지 않는다.
- 실제 Netlify 및 Android/iOS 재생 검증은 배포 후 별도로 수행해야 한다.
- 테스트: Node.js 24에서 `npm test` (외부 서버나 실제 데이터에 접근하지 않음).

참고한 공식 문서:
- https://docs.netlify.com/build/data-and-storage/netlify-blobs/
- https://docs.netlify.com/build/functions/configuration/
